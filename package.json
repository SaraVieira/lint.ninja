{
  "name": "wutthelint",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "eslint src",
    "start": "npm run -s serve",
    "build": "preact build --template src/index.html",
    "serve": "preact serve",
    "deploy": "npm run build && cp now.json build/now.json && cd build && now && now alias",
    "dev": "preact watch"
  },
  "alias": "wuththelint",
  "keywords": [],
  "author": "Sara Vieira",
  "license": "ISC",
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "devDependencies": {
    "eslint": "^4.3.0",
    "eslint-config-synacor": "^1.0.1",
    "flow-bin": "^0.51.1",
    "if-env": "^1.0.0",
    "preact-cli": "^1.4.1",
    "preact-cli-sw-precache": "^1.0.0"
  },
  "dependencies": {
    "axios": "^0.16.2",
    "capitalize-it": "^1.2.1",
    "preact": "^8.2.1",
    "preact-compat": "^3.16.0",
    "preact-router": "^2.5.5",
    "styled-components": "^2.1.1"
  }
}
